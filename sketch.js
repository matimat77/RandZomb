function preload() {}

function setup() {
  let canvas = createCanvas(640, 480);

  rectMode(CENTER);
  textAlign(CENTER, CENTER);
}

function update() {}

function draw() {
  update();
}

function keyPressed() {
  return false; // prevent any default behaviour
}
